# 实际缺陷与修订

## 2026-09-24 重制v2 / P1与角色管线试样

P1已由用户确认布局和人物大小。初次导航徘徊、码头坡道被地面覆盖、出生点与木桩投影重叠已修复，实际路线复测通过。仍存在灰盒檐棚/树/船/地形、静止角色、茶铺材质未精制和阴影颗粒；P1确认不表示这些美术问题通过。

整体步行图集试样 `hero-v2/motion-samples/walk-s-sheet-v1.png` 已真实生成，外观相对一致，但多数格仍重复同一领步脚，缺乏接触→经过→反脚接触的连续循环，拒收为正式步行。不是工具限额问题，不以文件数放行。

v2/v3定向编辑后，反脚接触出现，但经过姿势仍不足；固定网格编译显示两行脚底差4–6逻辑像素，Godot八帧固定变换实拍已留证。此生成路线三轮后暂停，不能据此填写48/192帧完成。

P2环境母版新增檐棚/石路/岸石，导出并实机复核；水面首轮有明显周期自投影条纹，降低扰动并关闭水面投影后已消除。其它灰盒、茶铺材料密度、阴影颗粒和石路自然边界尚未达到最终画面门槛。

## 2026-09-23 主角 v2 从源重做（本轮）

- 用户明确停止修补旧上传图，授权新建 `hero-v2.blend`。已按新的 `CHARACTER_PRODUCTION_STANDARD.md` 创建图像生成四视图、单一 Blender rig 和 idle/walk/thrust 动作、固定640×512 RGBA正交渲染配置；首帧160×128逻辑坐标的实际可见高度约80.75px，底端 y≈112.25。Blender只读审计：46个网格均绑定同一15骨骼 rig，只有一条剑刃，动作帧范围4/8/12，固定转向节点缩放1。
- 实际导出 S/SE 的14个抽样姿态，保存在 `art_source/linshui-quality/v1/characters/hero-v2/review-frames/`；不是48帧正式图集，也不是 Godot 截图。造型对四视图的还原仍不足：发型像硬盔、肩袖与白衣褶皱呈块状，脸和鞋在正常游戏尺度缺乏像素簇细节。
- `thrust` 由剑朝下修成前送后，S向命中帧仍与脸／前臂视觉重叠，SE剑尖读成斜上短刃，不符合“一眼看出直刺”的动作门槛。此为源姿态／造型问题，不能靠调 `pixel_size`、逐帧偏移、特效或切图掩盖。
- 内置 image_gen 生成 `idle-s-style-target-v2.png`，明显接近造型目标，但它是单帧1402×1122参考图，非同rig、非160×128，不能替换运行角色。完整提示词在同目录。v2 质量门判定：拒收，不导出批量48帧、不改 Godot 默认入口；继续在同一 v2 母版演进。

## 2026-09-23 本轮逐项复核

- 已修复：攻击图实际7排而非8排；按测得透明间隔重切42格，并重新序列化默认场景。南向、东南向各6帧的固定世界原点 Forward+ 截图见 `evidence/attack-row-fixed-*.png`，上下分裂不再出现。
- 已修复：上传角色重建脚本原先依赖缺少旧图集的 `quality-fixture.tscn`，会报资源缺失并超时。现在直接刷新现用预览场景。旧P2图集从现存同rig渲染源重编，三个 `.import` 恢复无损、禁自动VRAM重压缩；Godot资源合同重测通过。
- 已修复：按住空格的键盘自动重复曾可重启攻击动画。输入现在拒绝 `echo` 且在攻击期间不重新起手；资源合同测试注入自动重复和攻击中再次按键复验。
- 仍未修复：上传 `character5.png` 的剑尖横向越格，南向第5帧可在 `attack-row-fixed-s-4.png` 看到游离剑尖；NW 攻击缺失。上传图无可核实的共同 rig／授权。对南向六帧使用 imagegen 做了透明独立格候选，保存在 `.tmp/godot-quality/rejected-imagegen/attack-s-strip-v2.png`；虽然预览轮廓改善，但格尺寸、像素密度及同 rig 来源不能满足动作管线，未替换原图。
- 仍未修复：石路贴图接缝和重复。用当前底色图作参考生成的候选保存在 `.tmp/godot-quality/rejected-imagegen/stone-seam-v2.png`。逐边平均绝对RGB差：旧图左右约7.8、上下约9.4；候选左右约8.1、上下约15.9，反而更差，故不接入。
- G2仍拒收：角色肩袖／脸部像素簇、连续步态和出剑节奏、茶铺木纹与檐下动态受光、石路正式材质均未完成视觉验收；不以资源合同通过代替美术通过。

## 已修复并复测

- P1-001：码头返岸撞垂直岸壁。灰盒岸面挖出坡道缺口；失败报告保留在 p1-route-rejected-r1.json。
- P1-002：导航网格高于接地脚点约 0.2 m，路径容差过小导致坡道边折返。容差调整至 0.4 m；7 段实走通过；第二次失败报告保留。
- P2-001：方向帧重复。改为在同一母版外层转向节点旋转 rig 和网格，S/SE 内容哈希已不同。
- P2-002：Sprite3D offset Y 符号误用造成脚点在路面下。改为 (0,48)，以实机图复核；失败截图 p2-rejected-anchor-r1.png 保留。
- QA-001：Windows GUI Godot 子进程在 PowerShell 中异步返回，串接导入/构建/测试导致读到正在写的场景。改用 Python subprocess 的同步等待；资源合同测试重新通过，旧失败日志保留。

## 未修复／不得放行

见 p2-review.json。主角母版 r0→r1→r2→r3 均保存实际预览和修订记录。
本次不是缺 GPU 或图像生成工具；阻塞是已经观察到的美术质量差距和计划规定的三次修订评审门。
P2 夹具只有小型安全活动面，人工边界及两方向 Tab 切换明确属于验证工具，不冒充完整一屏导航或8向角色系统。
P1/P2 所有截图均来自 Godot Viewport；生成导引与 Blender 预览在源资产目录，不冒充实机证据。
