# P2 重制纵切：2026-09-24 实际检查

后续状态：用户选定保留设计、用可控骨骼验证；真实母版和22帧技术小样已完成，随后静止造型修订仍在整屏自检中拒收。当前判断以[造型门槛评审](style-gate-v2.md)及[STATUS.md](../STATUS.md)为准。下文“尚未执行”为当时记录。

结论：**未通过P2**。P1构图和尺寸已获用户确认；本报告只证明下述资产和检查实际存在，不代表整屏完成或商业品质。

## 产物

- `godot/linshui-quality/scenes/validation/composition-refined.tscn`：保持P1机位、地形碰撞与角色比例的独立P2场景。Godot打开后F6运行。WASD/点击可走，F3切换原地步行动作检查；默认main未替换。
- 檐棚：现有茶铺母版新增可编辑308对象，立柱、石柱础、梁枋、斜撑、椽子、真实厚度叠瓦；导出`tea-veranda-p2-v2.glb`，17640三角形。
- 石路：现有铺地母版新增170块独立倒角石材，导出`paving-layout-p2-v2.glb`，18360三角形。
- 河岸：同母版新增175个石砌/压顶对象，导出`bank-layout-p2-v2.glb`，18900三角形。保留码头坡道缺口。
- 真实生成的石灰岩底色：`art_source/linshui-quality/v1/textures/limestone-albedo-source-v2.png`，提示词同目录；Godot材质采用其精确副本。凹凸来自几何，未伪造或声称有配对法线图。
- 水面：原生Godot材质脚本，微弱动态法线及浅岸色差；第一轮自投影横条纹已通过关闭水面自投影、降低法线与反射强度消除。不是图像生成资源，不伪称真实流体模拟。

两个石材GLB各约4MB，因为嵌入真实底色图和编辑几何。此阶段用于原生Windows候选，未纳入网页/Pages；记录体积，P5再按实际显存、去重和压缩测试优化，不把未测预算称通过。

## 人物小样：3轮后拒收

主管线尚未冻结。此次试验为整组8帧图集生成，随后针对局部姿势编辑两次；不是逐帧生成，也不是肢体拼接。

1. v1：多数格同一只脚在前，无法形成循环。
2. v2：有了反脚接触，但经过姿势不足。
3. v3：接触/经过关系仍不明确。固定规则网格编译后，前排脚底约y113–114，后排约y108–109，约4–6逻辑像素差；会造成循环跳动。

原图/三个完整提示词、编译合同、原尺寸GIF位于`art_source/linshui-quality/v1/characters/hero-v2/motion-samples/`。**没有通过逐帧缩放/位移补偿脚点**。Godot单次pixel_size0.022、offset(0,48)、世界坐标固定，八张实际帧截图和变换日志在`evidence/p2-walk-s-v3-*`。

`walk-s-motion-sample-v3.png`位于review目录，只作缺陷诊断；不是正式角色，不能填充P2的48槽或P3的192槽。S/SE正式步行与直刺仍未完成。整组图集修正路线已到本轮三次检查上限，停止同策略重试，需要管线评审；不能因图片好看而放行。

下一路线建议：保持已选人物身份，先精制可控3D骨骼母版的单个正常尺寸姿态，再验证完整步行与直刺。旧低模已被拒收，不能直接启用；仍须形体和材质质量小样，运行时保持2D。此建议尚未执行，不计为成果。

## 验证

- P2完整物理路线、桥面/水/屋顶点击、输入锁、键盘行走与河岸防跌落通过，见`evidence/p2-refined-report-1080-routes.json`。
- 1080p/720p实拍与脚本退出码通过，见`reports/p2-refined-*-capture.json`，实际Vulkan Forward+ / RTX5060。
- 八帧实机采样见`evidence/p2-walk-s-v3-engine.json`，变换固定；图像内容的足点漂移仍拒收。
- `node scripts/run-quality-gate.mjs quick`完成32步骤、238测试全部通过，包含原发布资源检查。环境无npm命令，使用package.json中同一Node入口，不修改原测试。
- 292文件保护基线复核一致。未推送/上线。

## 剩余问题

人物连续性是当前核心质量阻塞。茶铺木材/瓦片、石路边界自然度、河岸层次与阴影采样仍需视觉打磨；其它桥/船/柳树/NPC等仍为灰盒。没有正式交互、声音、Windows包、90秒连续真实操作录屏或120秒Release性能结论。上述截图不能代替这些交付。
