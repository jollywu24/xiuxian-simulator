# AGENTS.md

本文件适用于仓库根目录及其所有子目录。若未来某个子目录新增更具体的 `AGENTS.md`，则该文件在对应子树内优先；用户的明确要求始终高于本文件。

## 1. 项目使命

本项目是《太虚命盘》（`xiuxian-simulator`）的可玩浏览器纵向切片。当前版本不是通用修仙沙盒，也不是完整宗门经营游戏；它用约 10～15 分钟验证以下核心闭环：

> 试命 → 死亡并理解因果 → 只带回一项成果 → 回到现实改命 → 越过旧死法并看见更深层问题

代理进行设计、编码或评审时，应把“玩家死后是否立刻想再试一次”视为最高层产品判断。新增功能至少应强化下列一项：

1. 第一次死亡意外但可理解；
2. 第二次尝试因旧情报而明显不同；
3. 道行、确证、命痕三类结算之间存在真实取舍；
4. 带回成果能尽快在现实中兑现；
5. 同一事件因词条、关系、准备或情报而出现不同解法。

若一项功能只增加数值、菜单、重复劳动或无关内容量，却不强化上述闭环，应缩小、改写或放弃。

## 2. 当前产品边界

当前仓库实现的是从 P0 核心循环、P1 情报驱动扩展到压缩 P2 黑日终局的完整浏览器 Demo，主要覆盖：

- 唯一自建主角与四种凡俗出身；
- 根骨、才性、因果三组先天词条二选一，以及一次整组重抽；
- 第一次模拟中的调查、关系、修炼和高风险试探；
- 第七日晚宴投毒、蒙面人补刀及死亡因果回溯；
- 道行、确证、按行为生成词池的命痕三类结算；
- 三类现实破局与“预知兑现”；
- 第二次模拟越过旧死法，进入完整乌铜矿因果事件；
- 传闻、确证、过期确证，以及天妒/偏差带来的规则变化；
- 证据驱动的同伴加入/拒绝、自主行动与短回合战斗；
- 矿底成果再次带回现实并即时兑现；
- 第一年第五年危机、四名核心 NPC、四种构筑与第七年黑日终局；
- 三个正式结局、现实锚点重试和二周目命痕继承；
- 本地自动存档、现实死亡重试、结算改选。

不要在没有明确任务的情况下扩展为宗门建造、弟子排班、材料仓库、境界刷级、开放世界任务板或随机事件拼盘。完整设计意图、后续 P1/P2 范围和验证指标以 `docs/GAME_DESIGN.md` 为准。

## 3. 技术栈与运行模型

- 原生 HTML、CSS、JavaScript；
- 浏览器原生 ES Modules（`.mjs`）；
- Node.js 内置测试运行器 `node:test`；
- 无构建步骤、无打包器、无第三方运行时依赖；
- 静态文件通过本地 HTTP 服务运行；不要直接用 `file://` 打开；
- 进度存储在浏览器 `localStorage`；
- 随机结果由种子驱动，可用 `?seed=balance-42` 固定。

除非任务确实需要且用户认可，不要引入框架、打包器、CSS 预处理器或新的 npm 依赖。新增依赖会改变项目“克隆后即可运行”的约束，必须说明收益、维护成本和替代方案。

## 4. 仓库地图

| 路径 | 职责 | 修改注意事项 |
| --- | --- | --- |
| `README.md` | Demo 范围、启动和验证入口 | 用户可见能力或命令变化时同步更新 |
| `package.json` | 最小脚本定义 | 保持 `type: module`；避免无必要依赖 |
| `web/index.html` | 页面壳、元信息、CSS/ESM 入口 | 保持 `lang="zh-CN"`、viewport、noscript 和模块入口 |
| `web/game-core.mjs` | 纯数据、确定性随机、开局词条和结算规则 | 不访问 DOM 或 `localStorage`；规则变更优先在此测试 |
| `web/app.mjs` | 全局状态、场景渲染、事件处理、存档和完整体验流程 | 保持状态转换显式；所有用户文本插值必须安全转义 |
| `web/styles.css` | 四套视觉模式、组件、响应式和动效 | 优先复用变量和现有组件类；同步检查桌面与移动端 |
| `tests/core.test.mjs` | 无依赖核心规则测试 | 固定种子；断言不变量而非脆弱的随机快照 |
| `scripts/cdp-smoke.mjs` | 通过 Chrome DevTools Protocol 跑完整浏览器冒烟 | `data-action`、屏幕文案或流程变化时同步更新 |
| `docs/GAME_DESIGN.md` | 完整设计基线与范围边界 | 设计方向冲突时先指出并确认，不要静默偏离 |

所有文本文件使用 UTF-8。中文乱码通常是终端读取编码问题，不要把乱码重新保存进仓库；在 PowerShell 中可显式使用 `Get-Content -Encoding utf8`。

## 5. 架构约定

### 5.1 纯规则层：`web/game-core.mjs`

该模块负责可独立测试的领域规则：

- `RARITY`：灰、白、蓝、紫、金的标签与排序；
- `ORIGINS`、`APPEARANCES`：角色创建静态数据；
- `OPENING_TRAITS`：先天词条数据；
- `SETTLEMENT_TRAITS`：后天命痕及行为标签、路线、权重；
- `hashSeed()`、`createRng()`：确定性随机；
- `generateOpeningSets()`：根骨、才性、因果三组稳定/高波动二选一；
- `scoreSettlement()`：按有效行为多样性和线索数评定结算；
- `deriveSettlementTraits()`：按种子、行为标签和评级生成不重复候选；
- 若干按 `id` 查询及标签去重函数。

保持以下不变量：

1. 同一输入种子和重抽次数必须产生同一开局；
2. 每组恰好两个不同候选，第一项稳定、第二项高波动；
3. 单次开局最多出现一个金色词条；
4. 一次允许的重抽应使用新的、仍可复现的随机流；
5. 结算候选不能重复；
6. 非丁级结算至少保证一个蓝色或更高候选；
7. 负面或高风险词条必须提供新路线、信息能力或规则变化，不能只是纯数值惩罚。

新增领域逻辑时优先写成纯函数并导出，再在 `app.mjs` 中接入。不要把可测试规则埋进点击处理器或模板字符串。

### 5.2 场景与状态层：`web/app.mjs`

`createInitialState()` 是存档结构的事实来源。当前关键状态包括：

- 角色、世界种子、开局抽取与已选词条；
- 命火、天妒、偏差、模拟次数；
- 行为标签、线索、先天词条触发记录；
- 第一次模拟的三个阶段选择；
- 结算评级、候选、带回成果与后天命痕；
- 现实破局、同伴选择、矿底选择；
- 时间线状态与轻量事件记录。

界面采用“状态 → 整页渲染”的简单模型：

1. 每个屏幕由一个 `renderXxx()` 函数返回 HTML；
2. `render()` 按 `state.screen` 选择渲染器；
3. `handlers` 以 `data-action` 名称映射到状态转换；
4. 容器上的单一点击监听器做事件委托；
5. `moveTo()` 更新屏幕并触发重新渲染；
6. 非首页屏幕渲染后自动保存。

现有主流程屏幕顺序为：

`landing → worldIntro → creator → openingTraits → birthSheet → encounter → encounterOutcome → arrival → omen → realityHub → sim1Morning → sim1Eve → sim1Feast → deathRecap → settlement → [traitDraw] → realityReturn → realityPlan → realityResolution/realityDeath → sim2Feast → sim2Road → companionResult → mineApproach → mineInvestigation → mineBattle → mineAftermath/mineDefeat → mineReturn → p1RealityPlan → ending → p2Interlude → buildChoice → year1Approach/year1Archive → year1Resolution → year5Hub/year5Crisis → blackSunPrep → finale → finalSummary → [cycleOpening]`

新增或修改流程时：

- 显式更新渲染器表和相应 handler；
- 对 handler 做当前屏幕或前置条件检查，避免重复点击破坏状态；
- 重要状态改变调用 `track()`，事件名使用稳定的英文 `snake_case`；
- 资源、线索、时间线与叙事结果应在同一次转换中保持一致；
- 不要直接依赖 DOM 作为业务状态来源；DOM 只是 `state` 的投影；
- 用户提供的姓名等内容必须经 `escapeHtml()` 后插入 HTML；
- 避免把未转义字符串拼进属性、选择器或脚本表达式。

### 5.3 存档契约

存档键为 `taixu-fateplate-demo-v1`（为兼容旧浏览器数据而保留），当前 `state.version` 为 `3`；`migrateSavedState()` 负责把 v1/v2 存档安全续接到 P1/P2。

- 首页不自动写存档，其余屏幕渲染后自动写入；
- 载入失败、JSON 损坏或版本不匹配时应安全回退；
- `new-game` 会清除旧存档；
- “现实死亡重试”和“改选第一次结算”只回退设计允许的部分，不应意外重置角色或已知因果；
- 更改状态结构时必须决定：兼容旧字段、迁移，还是提升版本并明确失效策略；
- 不要把 DOM 节点、函数、循环引用或不可序列化对象放入 `state`；
- 若改动结算回退逻辑，重点核对 `retry-settlement` 是否完整清理派生状态并保留第一次模拟线索。

### 5.4 稳定交互契约

`data-action`、`data-value`、`data-id`、`data-group` 和 `data-field` 不只是实现细节，也是浏览器冒烟测试的稳定钩子。

- 新交互优先沿用事件委托和语义化 `data-*`；
- 修改 action 名称时同步搜索并更新 `scripts/cdp-smoke.mjs`；
- 不要让测试依赖 CSS 布局层级或随机生成的 class；
- 数字键 1～4 会触发当前可用 `.action-card`，调整动作卡顺序时检查键盘行为；
- 禁用状态使用原生 `disabled`，不要只做视觉禁用。

## 6. 叙事与系统设计底线

编码实现必须尊重以下设计约束：

- 玩家始终只控制自建主角；NPC 可以同行、拒绝、隐瞒和自主行动，但不可切换控制；
- 模拟死亡与现实死亡必须明显不同。模拟死亡留下成果，现实死亡结束当前命途或回到明确锚点；
- 每种首次死亡至少提供一条可行动的新信息；
- 情报必须改变入口、敌人、对话、战斗规则或行动窗口，不能只是一段收藏文本；
- 玩家带回成果后，应尽快获得亲手兑现成果的机会；
- 旧死法被越过后要快速进入新问题，避免反复重走相同内容；
- 关键事件属于可追查因果链，而不是彼此无关的随机事件；
- 稀有度表示规则影响范围，不等于无条件更强；强力能力应有可感知代价；
- 自动文本只用于压缩无决策时段，关键发现必须来自可操作场景；
- 同一问题应尽量支持代价不同的多个解法，避免唯一标准答案。
- 新玩家进入游戏时按“世界与处境 → 宗门名牒 → 照骨验身 → 人物卡 → 首个事件 → 命盘异象”逐层揭示信息；不要在首页同时抛出未来事件、NPC 名单和系统术语；
- 面向玩家的界面必须保持世界内表达。禁止出现 `Demo`、`P0/P1/P2`、原型、测试、构筑、二周目、自动存档、菜单、养成等开发或产品描述；对应概念改写为法门、前世、命盘记录、现实锚点等叙事内语言；
- 新名词首次出现时必须由场景、人物或物件自然解释。尚未被玩家亲历或得知的时间线节点不得提前展示；
- 人物出场前先交代其身份与当前事件关系，避免连续抛出无上下文姓名；
- 人物创建应以宗门名牒和照骨镜呈现；人物卡至少显示身份、境界、持有物、凡俗牵挂与先天命签。
- 奇遇先展示可观察处境，再揭示人物身份；不得在选择前把幕后身份、完整奖励或标准答案写在界面上。
- 每条奇遇至少有三种带不同长期因果的处置，所得信物或关系必须在后续可操作场景中兑现，不能只进入收藏文本。

修改剧情文案时，同时检查：死亡回溯是否说清直接死因、已知前因、新问题和下次可改变之处；现实兑现是否能让玩家明确感到“因为我提前知道，所以这次不同”。

## 7. 视觉、响应式与可访问性

`web/styles.css` 通过 CSS 自定义属性和 `body[data-mode]` 维护视觉语义：

- `neutral` / 默认：角色创建和中性页面；
- `reality`：暖色实纸感；
- `simulation`：冷色墨迹感；
- `death`：暗红死亡态；
- `settlement`：玄金结算态。

工作规则：

- 优先修改根变量或现有组件类，避免散落的硬编码颜色；
- 新屏幕应在 `modeForScreen()` 中归入正确模式；
- 保持正文、弱化文字、边框、焦点态在四种模式中均有足够对比；
- 保留 `:focus-visible`，交互元素优先使用原生 `button`/`input`；
- 保持 `prefers-reduced-motion` 降级；
- 至少检查 1280×720 桌面视口和不大于 780px 的移动布局；
- 页面不应产生非预期横向滚动；
- 不要仅靠颜色表达稀有度、危险、选中或时间线状态；同时提供文本、标签、边框或结构提示；
- 新文案要注意中文断行，不要通过固定高度截断关键内容。

## 8. 开发命令

推荐 Node.js 20 或更新版本。

启动静态服务：

```bash
npm run serve
```

等价命令：

```bash
python3 -m http.server 8080 --directory web
```

Windows 上若没有 `python3` 命令，可使用：

```powershell
python -m http.server 8080 --directory web
```

打开：

```text
http://127.0.0.1:8080/
```

确定性调试：

```text
http://127.0.0.1:8080/?seed=balance-42
```

运行核心测试：

```bash
npm test
```

若环境只有 Node 而没有 npm：

```bash
node --test tests/core.test.mjs
```

仓库当前无 lint、format、build 脚本。不要在交付说明中声称运行了不存在的检查。

## 9. 测试策略

### 9.1 核心规则测试

以下改动必须新增或更新 `tests/core.test.mjs`：

- 随机算法、种子构成或重抽规则；
- 先天词条分组、稀有度限制或候选数量；
- 结算评分、行为标签、候选资格或保底；
- 去重、查询或其他可抽离的纯规则。

测试要求：

- 使用固定种子；
- 同时验证确定性和设计不变量；
- 不要依赖对象当前排列之外的偶然细节；
- 修复 bug 时先添加能够复现问题的断言；
- 所有测试应保持无网络、无浏览器、无第三方依赖。

### 9.2 浏览器冒烟

`scripts/cdp-smoke.mjs` 连接一个已开启远程调试端口的 Chrome 页面，当前覆盖：

- 首页无横向溢出；
- 创建角色和三组选词；
- 第一次模拟与死亡回溯；
- 确证路线的现实兑现；
- 第二次模拟、同伴回应、入矿调查、短战斗和矿底结算；
- 情报可信度变化、确证过期和 P1 现实兑现；
- P2 现实死亡与锚点恢复、三个正式结局、结局总结与二周目早期变化；
- `localStorage` 存档关键字段；
- 回退结算后选择命痕路线；
- 再次回退后选择道行路线；
- 页面运行时异常为空；
- 关键节点截图。

Linux 示例见 `README.md`。运行脚本前必须先启动静态服务，并让 Chrome 打开同一页面。若本地环境无法启动 Chrome 或 CDP，应明确说明“未运行浏览器冒烟”及原因，不能用核心测试代替后声称完整验证。

修改以下内容时应更新并运行冒烟：

- 屏幕顺序或状态转换；
- `data-action` 等交互钩子；
- 三类结算或现实破局路线；
- 存档结构及回退逻辑；
- 关键文案断言；
- 响应式布局或模式主题。

### 9.3 最小验证矩阵

| 变更类型 | 至少验证 |
| --- | --- |
| 文档 | UTF-8、命令和路径与仓库一致 |
| 纯规则/数据 | `npm test` 或等价 `node --test` |
| 场景文案但无流程变化 | 核心测试；手动打开受影响屏幕 |
| 状态机/交互/存档 | 核心测试 + 浏览器冒烟或等价完整手测 |
| CSS/布局 | 桌面与移动视口、四种受影响模式、键盘焦点、横向溢出 |
| 大范围改动 | 核心测试 + 完整冒烟 + 从新游戏开始的手动体验 |

## 10. 代理工作流程

### 开始前

1. 读取本文件、`README.md`、相关源码；涉及玩法方向时再读取 `docs/GAME_DESIGN.md` 的相关章节；
2. 检查 `git status --short --branch`，保留用户已有修改；
3. 用 `rg` 搜索所有引用，尤其是 action、screen、存档字段和文案断言；
4. 明确改动属于纯规则、状态机、表现层还是文档，避免把逻辑放错层；
5. 对较大任务列出短计划，并在实施中持续更新。

### 实施时

- 做最小但完整的改动，不顺手重构无关代码；
- 延续现有两空格缩进、分号、双引号和尾逗号风格；
- 标识符、事件名和代码注释使用清晰英文；面向玩家的文案使用自然简体中文；
- 数据对象保留稳定、可读的英文 `id`，显示名称与 `id` 分离；
- 复用 `actionCard()`、`traitCardHtml()`、`sceneHeader()`、`gameShell()` 等现有渲染帮助函数；
- 新规则尽量在 `game-core.mjs` 中纯函数化；
- 状态转换集中在 `handlers`，不要在渲染函数中偷偷修改状态；
- 保存兼容、测试钩子和确定性随机属于公共契约，变更前必须评估影响；
- 中文文件始终保持 UTF-8 与 LF 友好，不做无意义的全文件换行转换。

### 完成前

1. 查看 `git diff --check`，消除空白错误；
2. 查看 `git diff --stat` 和相关 diff，确认没有无关改动；
3. 按验证矩阵运行检查；
4. 对随机、资源、时间线、存档回退和重复点击做一次边界审视；
5. 用户可见能力或命令变化时更新 `README.md`；设计基线变化时更新 `docs/GAME_DESIGN.md`；
6. 最终回复先说明完成结果，再列出实际运行的验证与任何未验证项。

## 11. 完成定义

一项改动只有同时满足以下条件才算完成：

- 符合“试命 → 带回 → 改命”的项目目标和当前范围；
- 规则、状态、界面和文档彼此一致；
- 不破坏确定性随机、三选一结算、存档或回退契约；
- 对用户输入进行了安全处理；
- 桌面与移动布局没有明显回归，键盘交互仍可用；
- 相关自动化测试通过，未执行的高层验证被如实说明；
- 工作区没有因本任务产生的临时日志、截图、浏览器配置或生成物；
- 最终交付清楚描述改了什么、为何符合项目目标、如何验证。

## 12. 常见陷阱

- 不要使用 `Math.random()` 绕过种子系统；
- 不要用数组下标作为长期存档标识，使用稳定 `id`；
- 不要仅在 CSS 中隐藏无效操作，业务 handler 也要校验；
- 不要把用户姓名直接插入模板字符串而不调用 `escapeHtml()`；
- 不要改变 `data-action` 后忘记更新 CDP 冒烟；
- 不要添加存档字段后忘记处理“继续游戏”和“改选结算”；
- 不要把模拟获得的普通物品误写为可带回现实；当前每条命只能保留一种结算成果；
- 不要让先知情报只改变文案而不改变可选行动或规则；
- 不要让新路线延迟太久才兑现上一世成果；
- 不要把核心发现变成自动播放日志；
- 不要为追求“完整修仙感”稀释当前纵向切片；
- 不要提交 `/tmp` 截图、浏览器用户目录、日志、覆盖率目录或其他本地验证产物。
