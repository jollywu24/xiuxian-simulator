# AGENTS.md

本文件适用于仓库根目录及全部子目录。用户明确要求始终高于本文；若子目录出现更具体的 `AGENTS.md`，则在对应子树优先。

## 1. 项目使命

本项目是网络小说《文字武侠：开局破庙夜会帮主夫人》前期剧情的可玩浏览器改编。

核心体验是：

> 现实获得《武道》 → 创建弱小人物 → 看见全部奇遇条件 → 亲手支付条件与生死代价 → 把武功同步现实 → 隐藏或交换自己的特殊能力。

产品判断优先级：

1. 玩家是否先理解现实身份与《武道》的危险，再接触专有名词；
2. “逆天改命”是否改变可见条件和行动方案，而非自动发奖励；
3. 奇遇选择是否包含死亡、错过、关系和长期入口等不同结果；
4. 游戏所得是否很快在现实中得到亲手验证；
5. 小说人物与事件是否符合已核对的公开章节，而非只借用书名。

## 2. 改编基线

当前剧情基于原作前十章：

- 陈玄在江海大学暑假宿舍发现无法卸载的文字游戏《武道》；
- 武道人物默认名陈司命，主线路线为身世成谜、求道者；
- 专属命格“逆天改命”以五维归零为代价，允许重分已有属性并查看全部奇遇条件；
- 金陵东郊破庙有旅人遗物、沈氏承诺、神秘贡品三项固定奇遇；
- 《武道》武功、属性和境界会同步现实，人物只有两条初始命数；
- 青衣妇人是漕帮帮主夫人龙青鱼，并非受伤逃亡者；
- 龙青鱼因陆连山变心独自来到破庙，主线路线经“因爱成恨”“破庙夜话”获得鱼跃龙门诀；
- 心法在现实水下闭气时生效；
- 于可心、林毅代表龙国武道局登门登记新人。

不得重新引入归尘门、太虚命盘、晚宴投毒、乌铜矿、黑日灭门等上一版玩家剧情。

改编时使用原创转述，不复制小说正文长段落。若后续任务要求扩展新章节，应先从用户提供文本或合法公开来源核对人物、事件顺序和规则，再实现。

## 3. 玩家可见内容规则

- 所有玩家文本必须属于故事世界、人物卡、事件、条件、结果或可操作反馈；
- 禁止出现 Demo、P0/P1/P2、原型、测试、构筑、二周目、自动存档、菜单、开发进度等产品语言；
- 不在青衣妇人首次出现时提前显示龙青鱼、漕帮帮主夫人或陆连山；
- 新名词第一次出现时由场景、论坛、人物或物品解释；
- 成人情节保留人物动机与选择压力，不展示露骨内容；
- 不把旁白当玩法：关键发现必须来自按钮选择、条件板、资源变化或关系结果；
- 面向玩家的姓名等动态文本必须经过 `escapeHtml()`。

## 4. 当前流程

`landing → realityIntro → characterDraft → vow → destiny → characterSheet → templeWake → fateSight → allocation → templeTasks → forum → ladyArrival → ladyPressure → ladyTest → nightTalk → encounterReward → mindArt → realitySync → bureauDoor → ending`

可选支路：

- 明确死亡选择进入 `gameDeath`，失去一条命后返回 `ladyArrival`；
- 沉默避让进入 `quietDeparture`，允许回看另一条因果；
- 剩余一条命时，不再展示已经明确会直接死亡的重复危险选项。

新增场景时必须同步：

1. `renderers` 映射；
2. `handlers` 状态转移；
3. `screenMode()` 视觉模式；
4. 存档默认字段；
5. 浏览器冒烟流程；
6. README 与设计文档的用户可见范围。

## 5. 代码地图

| 路径 | 职责 |
| --- | --- |
| `web/index.html` | 页面入口、元信息、当前脚本入口 |
| `web/wudao-core.mjs` | 人物、五维、奇遇、关系、心法与武道局纯规则 |
| `web/wudao-app.mjs` | 状态机、渲染、事件委托与本地存档 |
| `web/styles.css` | 四种视觉模式、桌面和手机响应式 |
| `tests/wudao.test.mjs` | 当前剧情规则测试 |
| `scripts/cdp-smoke.mjs` | 桌面/手机完整浏览器流程 |
| `docs/GAME_DESIGN.md` | 剧情映射、玩法规则与后续边界 |

`web/app.mjs`、`web/game-core.mjs`、`tests/core.test.mjs` 是上一版迁移参考，当前网页不加载。除非用户要求清理旧实现，不要顺手重构或删除。

## 6. 规则层约定

可测试规则优先写入 `web/wudao-core.mjs`：

- `ATTRIBUTES`、`BACKGROUNDS`、`VOWS`：人物建档；
- `DESTINY`：逆天改命能力与代价；
- `TEMPLE_ENCOUNTERS`：破庙固定奇遇；
- `LADY_STAGES`、`NIGHT_TALK`：龙青鱼偶发奇遇；
- `MIND_ART`：鱼跃龙门诀；
- `allocateJadeBonus()`：玉佩三点属性重分；
- `templeTaskCost()`：不同分配对敲墙代价的影响；
- `resolveLadyChoice()`、`resolveNightTalk()`：关系与生死结果；
- `bureauConsequence()`：武道局登记取舍。

核心不访问 DOM、`localStorage` 或网络。所有返回对象保持可序列化。

## 7. 状态与存档

存档键为 `wudao-novel-route-v1`，当前 `version` 为 `1`。

`createInitialState()` 是状态事实来源，关键字段包括：

- 现实姓名、武道姓名、身世、初心；
- 命数、潜能、山桃、篝火时间；
- 五维分配与已完成固定奇遇；
- 论坛保密选择；
- 龙青鱼奇遇阶段、选择日志、好感与关系；
- 心法与现实同步状态；
- 武道局登记选择与结果。

handler 必须检查当前条件，避免重复点击重复领取潜能或奇遇物品。状态只能存放 JSON 可序列化值。

## 8. 交互与视觉

- 使用原生 HTML、CSS、ES Modules，无构建步骤和第三方运行时；
- 交互通过单一事件委托与稳定 `data-action`、`data-value` 完成；
- 数字键 1～9 触发当前可用 `.action-card`；
- 原生 `button`、`input` 保留键盘焦点；
- `neutral` 用于现实前提和人物创建；
- `simulation` 用于武道世界；
- `death` 用于人物死亡；
- `settlement` 用于奇遇结局和心法；
- `reality` 用于论坛、现实同步、武道局与结尾；
- 至少检查 1280×720 与 390×844；
- 不允许横向溢出，不以固定高度截断关键文案。

## 9. 验证

规则测试：

```bash
npm test
```

浏览器冒烟需要本地 HTTP 服务和开启远程调试端口的 Chrome：

```bash
node scripts/cdp-smoke.mjs 9225
```

冒烟至少覆盖：

- 首页无旧版剧情和产品词汇；
- 人物建档、逆天改命与人物卡；
- 两项固定奇遇和一项锁定条件；
- 青衣妇人身份延迟揭示；
- 一次人物死亡与命数减少；
- 主线路线到莫逆之交、鱼跃龙门诀；
- 现实闭气验证；
- 武道局登记结果；
- 本地存档恢复；
- 桌面与手机横向溢出；
- 页面运行异常为空。

## 10. 完成定义

一次改动只有在以下条件同时满足时完成：

- 符合已经核对的小说剧情或明确标注为后续原创扩展；
- 规则、状态、界面、测试与文档一致；
- 关键选择有真实后果，不只是不同文案；
- 新能力在短时间内提供亲手兑现机会；
- 玩家界面没有旧版剧情或产品术语；
- 规则测试和完整浏览器流程通过；
- 桌面、手机没有明显布局回归；
- 工作区没有浏览器配置、日志、截图等临时产物；
- 提交和发布状态如实说明。
