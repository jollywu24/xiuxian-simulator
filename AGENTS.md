# AGENTS.md

本文件适用于仓库根目录及全部子目录。用户的当前要求始终优先；若子目录存在更具体的 `AGENTS.md`，则只在对应子树覆盖本文。

本文只保存长期有效的项目约束和工作方法，不再复制完整剧情、场景跳转、存档字段、测试数量或像素清单。会频繁变化的事实必须回到代码、测试和对应设计文档中维护。

## 1. 项目定位

本项目是一款单人高武叙事 RPG：

- 文字、人物关系与状态系统是玩法核心；
- 2D 场景、热点和路线可视化负责建立空间感；
- 当前发布形态是原生浏览器游戏，优先保证手机横屏，同时兼容桌面与手机竖屏；
- 长期目标包含桌面封装与 Steam，但不能为尚未接入的平台破坏网页版本。

世界只发生在“大曜”高武江湖。玩家从低位人物开始，通过观察、选择、关系、武学、物品和战斗逐步取得行动权。

核心循环：

> 看见眼前局面 → 选择行动意图 → 由属性、武学、装备、关系、见闻与固定因果共同决定完成质量 → 承担真实后果 → 用新能力或新门路打开下一段局面。

产品判断顺序：

1. 玩家是否先看懂自己是谁、身在何处、眼前有什么麻烦；
2. 选择是否改变资源、关系、权限、地点或后续行动；
3. 失败是否带来新局面，而不是只让玩家重复读档；
4. 新武学、物品、见闻或关系是否在一至两个场景内得到亲手兑现；
5. 系统复杂度是否通过场景与反馈被玩家自然理解。

## 2. 不可突破的边界

- 只做武道世界，不加入现实世界、双世界或能力同步现实；
- 不加入论坛、武道局、新人登记、现代组织及其变体；
- 不加入其他玩家、多人竞争、玩家交易、排行榜或联网社会系统；
- 不重新引入归尘门、太虚命盘、晚宴投毒、乌铜矿、黑日灭门等旧版修仙剧情；
- 朝廷、山门、世家和帮会可以存在，但必须属于大曜江湖；
- 高武作品只用于研究结构和体验，不复制其专有人物、势力、境界、台词或剧情；
- 小说改编使用原创转述，不复制长段正文；未核对的内容必须标明为原创扩写，不能冒充原著事实；
- 成人情节只保留人物动机、关系压力和选择后果，不展示露骨内容。

当前稳定名词：

- 默认主角：陈司命，十六岁；
- 武境：未入门、锻体、聚气、通脉、宗师；
- 武学大类：心法、招式、轻功、锻体；
- 招式子类：剑法、刀法、拳掌、枪棍、暗器；
- 武学品阶：粗浅、寻常、上乘、绝学、镇派；
- 战斗用语：伤害、防御、减伤、远程；不要重新使用“攻势、防守、护身、远手”等旧称；
- 玩家可见的三种目标出身：世家旁支、市井子弟、身世成谜。

目标设计与已发布实现必须分开陈述。出身系统的现行规格见 `docs/systems/11_ORIGIN_PROLOGUES.md`，实际可玩状态以 `web/origin-core.mjs`、`web/wudao-app.mjs` 和测试为准，不得把设计文档中的待制作内容描述为已经上线。

## 3. 事实来源与冲突处理

| 需要确认的事实 | 第一来源 |
| --- | --- |
| 当前运行行为、状态版本、缓存版本 | `web/` 中实际加载的代码 |
| 规则是否成立、旧档是否兼容 | `tests/` |
| 当前开发入口、代码导航、文档去向 | `PROJECT_CONTEXT.md` |
| 人物、事件顺序、揭示边界 | `docs/STORY_BIBLE.md` |
| 产品体验与系统总边界 | `docs/GAME_DESIGN.md` |
| 当前优先级、已完成与待制作 | `docs/SYSTEM_ROADMAP.md` |
| 单个系统的规则与验收 | `docs/systems/` |
| 存档与桌面迁移 | `docs/SAVE_ARCHITECTURE.md` |
| 已确认决定的原因与取舍 | `docs/DECISION_LOG.md` |
| 已经交付的重要变化 | `docs/CHANGELOG.md` |
| 公开试玩说明 | `README.md` |
| 当前用户明确指定的阶段目标 | 对应 `/goal` 或用户当前要求 |

使用规则：

1. 用户最新决定高于仓库旧文档；
2. “现在已经能玩什么”以入口代码和测试为准；
3. “以后准备做什么”以路线图和系统文档为准；
4. `GOAL.md` 只有在用户明确要求继续该目标、且内容仍未完成时才作为当前范围，不能默认把历史目标当作下一步；
5. 效果图是视觉目标，不自动证明功能已经实现；运行截图也不能替代规则文档；
6. 发现冲突时先确认哪一份是旧信息，再同步修改所有受影响的事实源，不能只在一处打补丁。

禁止在本文重新维护以下易过期内容：

- 完整剧情节点链和所有支路；
- 当前存档版本号、测试总数、篇章数或节点数；
- 单个页面的完整像素尺寸与所有 CSS 比例；
- 某一阶段 `/goal` 的详细任务清单；
- 已完成系统的长篇功能枚举。

## 4. 玩家可见文本

所有玩家文本必须属于故事世界、人物、场景、事件、条件、结果或可执行反馈。

禁止出现在游戏中的产品语言包括但不限于：

- Demo、P0/P1/P2、原型、测试、构筑、开发进度；
- 车卡、自动存档、状态键、内容节点、流程回流；
- 现实世界、论坛、武道局、其他玩家。

写作方法统一采用“具体经历式描述”：

> 生活环境或眼前所见 → 做过或正在发生的具体事情 → 因此知道或能够做到什么。

执行标准：

- 用动作、物件、地点和生活细节证明能力，不用抽象标签替角色下结论；
- 出身只交代经历、见识、权限和代价，不替玩家决定善恶与性格；
- 避免对仗口号、抽象概念堆叠、连续四字短语和刻意的“不是……而是……”；
- 替换姓名后仍适用于任何武侠角色的句子，必须改得更具体；
- 旁白可以有武侠小说或说书人的节奏，但首先要清楚、自然、可行动；
- 详细方法、反例和检查表见 `docs/systems/08_CONTENT_PIPELINE.md`。

信息揭示必须遵守人物认知：

- 未见过的人物不提前显示姓名、身份或关系；
- 未查明的地点、势力、物品来历和幕后动机不提前剧透；
- 新名词第一次出现时由场景、人物或物品自然解释；
- 青衣妇人首次出现时不提前显示龙青鱼、漕帮帮主夫人或陆连山；
- 出身选择阶段使用“世家旁支”，正式进入相关剧情前不显示“沈家”；
- 出身选择界面不展示主角立绘、世界观长文、初始物品清单、未来任务或隐藏风声。

选择与反馈：

- 选项直接标明有关的属性、武学、装备、关系、见闻、资源或兵器条件；
- 条件详情可以展开查看依据；
- 不可用选项保留并置灰，明确说明缺少什么；
- 重要、危险和不可用状态不能只靠颜色，必须同时有文字或图形标识；
- 玩家选择落定后写入右侧叙事信息流，结果紧随其后，旧记录自然上推；
- 动态姓名和其他外部输入必须经过 `escapeHtml()`。

## 5. 系统设计原则

### 5.1 局面与选择

- 用“稳定事实、人物欲望、可用手段、代价、失败推进、共同回流”组织局面；
- 玩家决定行动意图，固定因果骰只决定完成质量，不替玩家决定立场；
- 同一状态与同一行动必须得到同一因果结果，禁止通过读档刷新点数；
- 普通失败继续推进并留下代价；只有明确死局或累积到致命后果时才进入死亡；
- 核心选择必须在下一场景产生人物、地点、权限、资源或风险上的可见回响；
- 关系不是单纯好感条，还应改变透露、传授、引见、包庇、牺牲和求助权限；
- 身份使用离散社会位置，不做另一条经验条；
- 世界只模拟能形成选择的部分，不为完整感提前建设全 NPC 日程、随机物价或通用天气。

### 5.2 成长、武学与装备

- 阅历是玩家可见的武学研习资源；内部旧字段 `potential` 只作为兼容镜像，不在新界面继续扩散；
- 已学武学与当前携带武学分开；战斗只读取携带内容；
- 真气在聚气后才可用，未入门和锻体阶段不伪造真气数值；
- 真气催动强化原动作，沿用同一判定，不复制一套“强化技能”按钮；
- 装备、人物属性、境界、武学和伤势必须接入同一战斗公式；
- 不建立与现有引擎并行的第二套战斗公式；
- 装备首版不做耐久、随机词缀、强化、套装和繁复部位扩张；
- 新能力至少在战斗、调查、旅行、关系或制度行动中的两类场景产生用途。

### 5.3 状态与存档

- `createInitialState()` 是运行状态默认值的事实来源；
- 状态只保存 JSON 可序列化值；
- 带奖励的效果必须有稳定 ID、保持幂等，handler 必须防止重复领取；
- 内容字段变化时先设计迁移，再提升状态版本；
- 不改变现有存档主键，不用新空档覆盖旧档或损坏档；
- 浏览器主档、校验、备份与桌面原子文件规则见 `docs/SAVE_ARCHITECTURE.md`；
- 修改人物、装备、武学、战斗或剧情状态后，必须检查旧版本迁移。

## 6. 技术架构

项目使用原生 HTML、CSS 和 ES Modules，无构建步骤和第三方运行时。

| 路径 | 职责 |
| --- | --- |
| `web/index.html` | 正式网页入口和发布资源引用 |
| `web/wudao-app.mjs` | 状态机、渲染、事件委托和系统编排 |
| `web/appearance-core.mjs` | 容貌目录、默认值、迁移规范化和正式资源映射 |
| `web/origin-core.mjs` | 三出身、独立序章、旧ID迁移、任务结果与个人事件 |
| `web/wudao-core.mjs` | 世界、人物建立与首章基础规则 |
| `web/wudao-p0-core.mjs`、`web/wudao-p1-core.mjs` | 后续篇章和跨系统纯规则 |
| `web/content/` | 内容目录、节点和静态事实 |
| `web/character-system.mjs` | 人物派生、装备和统一战斗数值 |
| `web/martial-system.mjs` | 武学目录、养成、携带和战斗映射 |
| `web/combat-*.mjs` | 通用战斗、遭遇定义与独立演武入口 |
| `web/inventory-core.mjs` | 行囊目录、分类、品质和使用条件 |
| `web/save-*.mjs`、`desktop/` | 浏览器存储与未来桌面文件适配 |
| `web/wudao-scenes.mjs` | 场景物件、人物站位和已知路线 |
| `web/assets/`、`web/styles.css` | 正式美术资源与响应式界面 |
| `tests/` | 纯规则、迁移和系统回归 |
| `scripts/` | 内容校验与浏览器完整流程 |
| `docs/` | 剧情、系统、路线图和参考拆解 |

`web/app.mjs`、`web/game-core.mjs` 和 `tests/core.test.mjs` 是旧版迁移参考，正式入口不加载。除非用户明确要求清理，不要顺手重构、删除或继续在其中开发。

架构约定：

- 纯规则模块不得访问 DOM、`localStorage` 或网络；
- 规则返回值保持可序列化，并优先由测试直接验证；
- 页面交互继续使用统一事件委托和稳定 `data-action`、`data-value`；
- 新场景必须同步渲染映射、handler、视觉模式、默认状态、必要的场景物件／路线、测试和文档；
- 内部英文状态键不得直接进入玩家界面；
- 修改运行时代码后，检查 `web/index.html` 及模块 import 的发布版本参数，统一递增，避免手机缓存混用新旧模块；
- 主模块启动失败时必须保留可见的恢复入口，不能只显示空白页。

## 7. 界面与美术

- 桌面与手机横屏以“左侧场景、右侧叙事信息流”为主要布局；
- 手机竖屏按场景、叙事、行动顺序排列，不得横向溢出；
- 人物、行囊、武学是三个同级入口，打开后覆盖完整设备视口；
- 人物页不显示命灯、阅历或装备耐久；显示境界、气血、真气、防御、减伤、特性名称和装备；
- 装备点击后先打开详情，再由详情中的装备、替换或卸下按钮确认；
- 行囊和装备的名称、品质文字与品质底框使用同一品质色；
- 武学空位、图标和装备格保持一致的视觉尺寸，不以拉伸素材填满格位；
- 场景热点必须对应原图物件，点击场景空白处关闭“所见”卡片；
- 火势、饥饿等短时变化使用场景中央飘字，最终状态留在固定区域；
- 原生按钮和输入框保留键盘焦点，数字键 `1`～`9` 继续触发当前可用行动；
- 视觉层级、最新确认效果和详细响应式标准见 `docs/systems/07_INTERFACE_SAVE_QA.md` 及用户最近确认的效果图；
- `web/assets/UI_Renderings/` 中的图片默认是设计参考，不应被网页直接加载，除非已经明确转化为正式运行资源；
- 新运行资源应放入对应资产目录，控制文件体积，优先使用适合网页的格式，并在目标视口实际检查裁切与清晰度。

最低检查视口：

- `1280×720` 桌面；
- `390×844` 手机竖屏；
- `844×390` 手机横屏；
- 涉及已确认横板效果时，再检查设计标尺视口。

任何视口都不允许横向溢出，也不以固定高度截断关键文案或操作按钮。

## 8. 改动流程

开始修改前：

1. 查看 `git status --short`，现有修改和未跟踪文件默认属于用户；
2. 查阅受影响模块、测试和权威设计文档；
3. 区分“当前已实现”与“目标设计”，不要根据效果图或路线图假设代码已经存在；
4. 识别存档、手机布局和剧情连续性风险；
5. 选择能在当前剧情中验证价值的最小纵切，不先建设脱离场景的完整大系统。

实现顺序通常为：

1. 纯规则和数据；
2. 状态默认值与迁移；
3. 渲染与交互；
4. 场景／美术资源；
5. 自动测试与浏览器流程；
6. README、剧情文档、系统文档和路线图。

编辑约束：

- 不覆盖、删除或提交无关的用户改动；
- 不用大规模重构掩盖一个局部需求；
- 新增内容节点和效果使用稳定 ID；
- 选择结果必须落入状态，不做只换文案的伪分支；
- 引用小说内容前先核对用户提供文本或合法公开来源；无法核对时明确作为原创扩写；
- 修改玩家文本时同时检查认知边界、术语一致性和“具体经历式描述”。

## 9. 验证

完整发布前验证：

```bash
npm run verify
```

局部诊断入口：

```bash
npm test
npm run validate:content
npm run test:release
npm run smoke
npm run smoke:origins
npm run smoke:combat
```

测试层级、自启动浏览器、覆盖矩阵和失败定位以`docs/TESTING.md`为事实源；本文件不维护脚本参数清单。

验证范围按改动选择，但不能低于：

| 改动类型 | 必须验证 |
| --- | --- |
| 文档或文案规范 | `git diff --check`、规则测试、内容校验 |
| 纯规则／状态／存档 | 对应测试、全量规则测试、内容校验、迁移路径 |
| 剧情节点 | 内容校验、相关规则测试、从可达前置进入并走到后续回流 |
| 人物／行囊／武学界面 | 桌面、手机竖屏、手机横屏、打开／关闭／切换／确认操作 |
| 场景或热点 | 原图坐标、空白关闭、缩放裁切、横竖屏 |
| 战斗 | 对应引擎测试、固定因果、敌方阶段、战果承接、演武流程 |
| 运行时代码或资源 | 发布版本参数一致、页面无运行异常、手机缓存刷新后可进入 |

不得声称没有实际执行的测试已经通过。若浏览器环境或调试端口不可用，应如实说明未完成的验证，不用规则测试代替视觉检查。

## 10. 发布与完成定义

- `main` 是正式网页发布分支，推送后由 `.github/workflows/pages.yml` 部署 `web/`；
- Pages 部署必须先通过 `.github/workflows/test-web.yml` 的完整质量门禁；部署后再验证线上构建号、资源契约和真实浏览器启动，细则见 `docs/RELEASE.md`；
- `?debug=1` 的 `window.WudaoDebug` 只服务自动回归，普通玩家入口不得注入该对象或出现调试控件；
- 可选战斗预览会由发布流程挂到独立路径，不得替换正式根页面；
- 每次完成修改并通过相应验证后，直接提交并推送当前发布分支，除非用户明确要求只保留本地；
- 只暂存本次范围内的文件，不把无关截图、日志、浏览器配置或用户素材带入提交；
- 推送后确认本地 `HEAD` 与远端目标分支一致；
- 发布状态必须如实说明，不把“已提交”“已推送”“Pages 已部署”混为一谈。

一次改动只有在以下条件同时满足时才算完成：

- 符合纯高武、单人叙事边界；
- 规则、状态、界面、测试与文档一致；
- 玩家选择产生真实且可见的后果；
- 新能力在近期场景中得到兑现；
- 玩家界面没有旧剧情、现代线、内部状态键或产品术语；
- 旧存档没有被无意破坏；
- 桌面与手机没有明显布局回归；
- 自动验证通过，必要的浏览器流程已实际检查；
- 工作区只剩用户原有或明确保留的无关文件；
- 提交、推送和部署状态说明准确。

## 11. 本文件的维护原则

只有长期规则变化时才修改 `AGENTS.md`。以下变化应更新各自事实源，而不是继续扩充本文：

- 新剧情、人物或揭示顺序：`docs/STORY_BIBLE.md`；
- 当前开发导航或文档去向：`PROJECT_CONTEXT.md`；
- 系统完成状态和下一优先级：`docs/SYSTEM_ROADMAP.md`；
- 具体系统公式、字段或界面规格：`docs/systems/`；
- 存档版本和迁移：代码、测试与 `docs/SAVE_ARCHITECTURE.md`；
- 自动化测试与浏览器回归：`docs/TESTING.md`；
- 调试状态协议：`docs/DEBUGGING.md`；
- Pages、资源契约与线上冒烟：`docs/RELEASE.md`；
- 新的长期设计／工程取舍：`docs/DECISION_LOG.md`；
- 已交付的重要变化：`docs/CHANGELOG.md`；
- 当前试玩能力和链接：`README.md`；
- 单次施工范围：当前 `/goal`。

若一条规则必须频繁随功能改动更新，它就不适合继续写在本文件中。
