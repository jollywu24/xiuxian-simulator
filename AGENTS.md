# AGENTS.md

适用于仓库及其子目录。系统、开发者指令和工具权限优先；在此范围内，用户最新要求高于项目旧文档，子目录规则只覆盖对应子树。本文规定执行边界，专题文档提供按需使用的细则。

## 执行约定

- 行动请求直接做到交付；只请求咨询、审阅或方案时不擅自实施。普通实现选择自行决定，不停在计划或“可以继续”。
- 先查现有上下文与代码。仅在无法推断的缺口会改变目标、操作对象、授权范围或造成重大返工时澄清；合并必要问题，等待时继续独立工作。可逆偏好采用项目惯例，不能编造关键事实。
- 同一任务中已明确的授权持续有效，不重复确认。确需批准时先完成可独立进行的准备，给出具体对象和影响；沉默不算批准，业务授权不替代工具提权。
- 修改前查看 `git status --short`，保留用户已有修改和未跟踪文件，只改本次范围。不要为清理工作区删除、覆盖或提交无关内容。
- 优先复用现有方案；新设计或实质重做才补充成熟参考与采用取舍，记录到对应系统文档或决策日志，普通修复不重走调研。先验证最小纵切，再继续完成原范围，不擅自扩大成通用框架。
- 失败按证据诊断；没有新信息时不反复重试。只暂停真正受阻的部分，完成独立工作，明确剩余依赖；不能把阶段产物称为全部完成。

## 项目边界与事实来源

- 只做“大曜”单人高武江湖。
-- 旧版叙事 RPG 与 2D 沙盒分开维护，旧版布局、剧情及 Three.js 架构不自动约束新沙盒。已实现和已上线以实际入口、代码、测试及部署证据为准，设计稿和路线图不代表实现。
- `GOAL.md` 仅在用户明确要求继续其中未完成目标时生效。事实冲突用证据解决，并在允许范围内同步直接相关文档；无关历史问题不扩大当前任务。

## 按需读取

开始相关改动前，读取下列文档的对应章节；不要每次加载全部文档。详细规范仍有效，仅在涉及该领域时触发。

| 任务 | 入口 |
| --- | --- |
| 定位代码、系统规格与文档职责 | [PROJECT_CONTEXT.md](PROJECT_CONTEXT.md) |
| 沙盒研发与阶段验收 | [沙盒定稿入口](docs/sandbox/README.md) |
| 旧版玩法、文案、状态、架构或界面 | [旧版研发约束](docs/LEGACY_DEVELOPMENT.md)，按目录选择相关章节 |
| 剧情与内容生产 | [剧情圣经](docs/STORY_BIBLE.md)、[内容管线](docs/systems/08_CONTENT_PIPELINE.md) |
| 存档与迁移 | [存档架构](docs/SAVE_ARCHITECTURE.md)；不得改主键或用空档覆盖旧档／损坏档 |
| 资源生成、替换、移动或删除 | [资源规范](docs/ASSET_POLICY.md)；已确认效果图另读 [还原流程](docs/VISUAL_IMPLEMENTATION.md) |
| 测试与浏览器验收 | [验证规范](docs/TESTING.md) |
| 提交、推送与部署 | [发布规范](docs/RELEASE.md) |

## 验证与完成

- 按实际影响验证。纯文档／指令检查差异、引用与一致性，不运行无关游戏测试；运行时先跑相关检查，混合改动或影响不明时用 `npm run verify:quick`，界面补对应浏览器验收。
- 相关检查通过后，只因新改动、失败、未解决风险或发布门禁扩大／重复验证。已授权发布 `main` 或明确要求完整验收时运行 `npm run verify`；本地检查点不自动触发发布。
- 提交、推送和部署按实际授权执行。已要求发布就完成到线上核验，不重复询问；普通修改或不含发布的目标完成不自动授权推送。不得声称未执行的验证或部署已经通过。
- 完成意味着原请求的产物、行为与适用验收均满足，相关事实源一致。必要工作不能改列后续建议，可选打磨不阻碍收尾；最终简述结果、验证、剩余限制及发布状态。

本文只保留共同边界与阅读入口。领域细则、术语、像素参数、测试矩阵及阶段状态写到对应文档，不再回填；同一规则只维护一处。
