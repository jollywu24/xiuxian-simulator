# AGENTS.md

本文件适用于仓库根目录及所有子目录。若未来子目录新增更具体的 `AGENTS.md`，则子目录文件在对应范围内优先；用户明确要求始终具有最高优先级。

## 1. 项目使命

本仓库是《太虚命盘》的浏览器可玩纵向切片。产品定义是：

> 一款把每次死亡都炼成破局条件，最终亲手接管灭门因果的修仙人生模拟 Roguelite。

当前 P0 用约 10～15 分钟验证单一“晚宴投毒”因果链。核心不是内容量、数值成长或随机奖励，而是：

> 选择试命命题 → 亲历未来 → 自动保留全部记忆 → 固命或化劫 → 补齐目标命途条件 → 利用并接管旧死局。

代理设计、编码或评审时，首先判断玩家是否能清楚回答：

- 我这一世在验证什么？
- 我亲历后确认了什么、猜错了什么？
- 我想抵达哪个命途，还缺哪些条件？
- 固命与化劫为何是一次真实取舍？
- 我怎样从受害者成为敌方机制的新操作者？

只增加数值、菜单、重复劳动或无关内容量的功能，应缩小、改写或删除。

## 2. 当前产品边界

当前可玩 P0 必须包含：

- 祖师洞灭门预兆；
- 至少两个明确试命命题；
- 晚宴前调查、主动收束与死亡深入；
- 见闻、推测、已验证事实、过期情报；
- 亲历信息自动保留，不参与奖励竞争；
- 固命/化劫两类结算；
- 观命、藏锋、守命、借劫四个命痕族的最小样本；
- 同一行动随能力产生不同深度；
- 命途条件板与受劫至驭劫的掌控升级；
- 水务流程制度漏洞；
- 与闻青禾的单向前世关系；
- 假死追凶、敌方口令、假回执和反向名单；
- 主动且不可逆的新锚点选择；
- 自动存档、桌面和移动端适配、浏览器冒烟测试。

当前 P0 明确不做：

- 完整捏人、外观和出身系统；
- 随机先天词条与重抽；
- 完整战斗、装备和境界刷级；
- 天妒、复杂偏差、多周目；
- 乌铜矿、七年事件链和三个正式结局；
- 大地图自由移动与全量 NPC 状态机。

这些内容可以保留在 `docs/GAME_DESIGN.md` 的正式版设计中，但未经明确任务不得重新塞回 P0。

## 3. 不可违反的设计规则

### 3.1 记忆默认保留

玩家亲历的对话、时刻、地点、行为与死亡过程自动进入见闻账。不得把情报设计成与修为或命痕互斥的普通奖励，也不得因玩家未选某个结算卡而让角色失忆。

信息可变为过期，但不得被静默删除。世界线偏转后，精确时序可以失效，底层因果仍应保留追查价值。

### 3.2 结算只有两种人生方向

- 固命：带回本世实际得到的能力、抗性、技艺或极少数物品；
- 化劫：按行为、死法和选择生成后天命痕。

禁止恢复“道行/确证/词条三选一”。化劫候选必须可追溯到真实行为标签；固命成果必须来自本世实际经历。

### 3.3 实验便宜，写回昂贵

从当前锚点进入浅层模拟免费。命火用于固命、化劫、进入新深度和建立锚点。不要用启动成本惩罚试毒、故意入局和观察坏结局。

### 3.4 行动有深度，不是钥匙门

基础行动原则上始终可尝试。词条、固命成果、关系与准备影响观察深度、代价和结果，不应简单控制按钮出现或消失。只有物理、时间或叙事上确实不可能的行动才能禁用。

### 3.5 事件必须能被接管

事件设计至少考虑避开、阻止、利用和接管四层结果。最高阶奖励优先是渠道、权限、口令、名单、流程或敌人计划，而不是经验值或“事件完成”。

### 3.6 制度是玩法

每个核心事件至少有一种制度漏洞路线。归尘门所有日常制度都可能是祭阵的一部分。底层规则“祭阵只能收割被宗门正式承认身份的人”不得随意改动；新增规则必须简单、意外、可解释制度且可被玩家多次利用。

## 4. 技术栈

- 原生 HTML、CSS、JavaScript；
- 浏览器原生 ES Modules（`.mjs`）；
- Node.js 内置 `node:test`；
- 无构建步骤、无打包器、无第三方运行时依赖；
- 静态文件通过本地 HTTP 服务运行，不使用 `file://`；
- 状态存于浏览器 `localStorage`；
- 当前存档键为 `taixu-fateplate-demo-v2`，状态版本为 `4`。

未经用户明确同意，不引入框架、打包器、CSS 预处理器或 npm 依赖。新增依赖必须说明收益、维护成本和无依赖替代方案。

## 5. 仓库地图

| 路径 | 职责 | 修改要求 |
| --- | --- | --- |
| `README.md` | P0 范围、启动与验证 | 用户可见能力变化时同步更新 |
| `web/index.html` | 页面壳、元信息、CSS/ESM 入口 | 保持 `lang="zh-CN"`、viewport、noscript |
| `web/game-core.mjs` | 命题、见闻、命痕、行动深度、命途条件纯规则 | 不访问 DOM/localStorage；规则变更优先在此测试 |
| `web/app.mjs` | 状态、场景渲染、转移、存档 | 状态转移显式；用户文本安全转义 |
| `web/styles.css` | 四种视觉模式、组件、响应式 | 复用 CSS 变量；同时检查桌面和移动端 |
| `tests/core.test.mjs` | 无依赖核心规则测试 | 固定输入，断言设计不变量 |
| `scripts/cdp-smoke.mjs` | CDP 真实浏览器完整流程 | action、文案断言或屏幕顺序变化时同步 |
| `docs/GAME_DESIGN.md` | 唯一完整产品设计基线 | 设计冲突时先指出并确认，不静默偏离 |

所有文本文件使用 UTF-8。PowerShell 读取中文时显式使用 `Get-Content -Encoding utf8`；终端乱码不代表源文件损坏，禁止把乱码重新保存到仓库。

## 6. 核心规则层约定

`web/game-core.mjs` 当前公开领域对象：

- `INFO_LEVELS`：见闻、推测、已验证事实、过期情报；
- `MASTERY_STAGES`：受劫、识劫、避劫、破劫、借劫、驭劫；
- `THESES`：试命命题及解锁条件；
- `FATE_MARKS`：四个构筑族的后天命痕；
- `FATE_PATHS`：避宴、提前换水、假死追凶、反向名单；
- `createMemory()` / `upsertMemory()` / `ageMemories()`：信息生命周期；
- `evaluateThesis()`：命题报告与亲历记忆；
- `deriveFateMarks()`：行为驱动命痕候选；
- `getActionDepth()`：同一行动的构筑深度；
- `evaluatePathConditions()`：命途条件状态；
- `getMasteryStage()`：掌控阶段。

保持以下不变量：

1. 更低可靠度的信息不能覆盖已验证事实；
2. 过期情报保留但不能满足精确的当前条件；
3. 新命题由亲历记忆解锁，而非结算奖励类型；
4. 主动收束与死亡都回答命题，死亡提供更深机制；
5. 命痕候选不重复已拥有命痕；
6. 行动深度至少有基础尝试，不因缺少词条消失；
7. 命途板返回每个条件的满足状态；
8. 高阶命途在亲历前保持部分隐藏；
9. 反向名单必须依赖敌方联络、假回执和身份规则；
10. 掌控阶段顺序固定为受劫至驭劫六级。

新增可测试规则时先写成纯函数并导出，再接入 `app.mjs`。不要把领域判断埋进点击处理器或 HTML 模板字符串。

## 7. 应用状态与场景流

`createInitialState()` 是存档结构的事实来源。关键状态包括：命火、心神负担、模拟次数、掌控层级、当前命题、本世行为、见闻、命痕、固命成果、现实准备、夺取标记、历史、偏差和锚点。

当前主流程：

`landing → realityHub → trialBrief → simObserve → simDecision → recap → settlement → [markDraw] → fateBoard → 第二次试命 → fateBoard → tracked → takeover → anchor → final`

`simDecision` 可选择主动收束或继续至死亡。`fateBoard` 允许执行避宴、换水、假死或继续试命；不要假设所有玩家走唯一顺序。

修改流程时：

- 显式更新 renderer 表和 handler；
- handler 检查当前屏幕、前置条件或禁用状态；
- 资源、记忆、掌控与叙事结果在同一次转移中一致更新；
- DOM 不是业务状态来源；输入仅在明确表单事件中写回状态；
- 用户姓名等文本插入 HTML 前必须调用 `escapeHtml()`；
- 非首页渲染后自动保存；
- 技术自动存档与叙事现实锚点保持概念分离。

## 8. 稳定交互契约

`data-action`、`data-value`、`data-field` 是浏览器冒烟测试与键盘交互的稳定 API。

- 新交互优先使用事件委托和语义化 `data-*`；
- action 改名时同步更新 `scripts/cdp-smoke.mjs`；
- 禁用使用原生 `disabled`，不能只做视觉禁用；
- 数字键 1～9 触发当前可用 `.action-card`；
- 测试不依赖脆弱 CSS 层级或随机 class；
- 弹窗或输入界面不得吞掉必要键盘焦点反馈。

## 9. 视觉与可访问性

四种视觉语义：

- `neutral`：首页；
- `reality`：暖纸现实；
- `simulation`：冷墨模拟；
- `death`：暗红死亡；
- `settlement`：玄金结算。

工作规则：

- 优先修改根变量和现有组件，不散落硬编码颜色；
- 条件板可以较密集，普通场景保持单一主决策区；
- 保持 `:focus-visible`、原生 button/input 与 reduced-motion；
- 至少检查 1280×720 和 390×844；
- 页面不得产生非预期横向滚动；
- 不只靠颜色表达可靠度、满足、危险或选中；
- 中文关键文案不得被固定高度截断；
- UI 应像命盘、命簿和宗门文书，不像 SaaS 管理后台。

## 10. 测试要求

核心规则变化必须更新 `tests/core.test.mjs`。至少覆盖信息升级/过期、命题解锁与报告、命痕生成、行动深度、条件板、隐藏路线和掌控阶段。

状态机、交互、存档、关键文案或响应式布局变化必须运行浏览器冒烟。当前冒烟必须完成：

- 第一命题与模拟死亡；
- 见闻自动保留；
- 固命；
- 现实准备和命途缺口；
- 第二命题与化劫；
- 假死追凶、夺法、反向名单；
- 锚点与最终总结；
- 本地存档重载；
- 桌面和移动端无横向溢出；
- 页面运行时异常为空；
- 关键节点截图。

推荐命令：

```bash
npm test
```

或：

```bash
node --test tests/core.test.mjs
```

仓库没有 lint、format 或 build 脚本。交付时不得声称运行了不存在的检查。若环境无法启动 Chrome/CDP，应明确说明未运行浏览器冒烟及原因，不能以核心测试冒充完整验证。

## 11. 内容评审清单

新增事件或分支必须检查：

- 命题是否具体可验证；
- 首次死亡是否意外但可理解；
- 报告是否说明直接死因、错误判断、观察和缺口；
- 所有亲历信息是否自动可用；
- 固命与化劫是否都来自本世行为；
- 基础行动是否可尝试、构筑是否增加深度；
- 是否至少有两种代价不同的解法；
- 是否有制度漏洞路线；
- 是否支持避劫、破劫、借劫和驭劫的升级；
- NPC 是否按动机和底线应变；
- 单向前世关系是否既有优势也有情绪代价；
- 最高阶结果是否夺取了敌方机制；
- 锚点是否明确固化损失。

## 12. 完成定义

任务只有在以下条件同时满足时才算完成：

1. 实现与 `docs/GAME_DESIGN.md` 一致；
2. 受影响文档、规则、界面和自动化同步更新；
3. 相关核心测试通过；
4. 状态机或 UI 变化已完成浏览器验证，或明确记录无法验证的原因；
5. 桌面与移动端没有明显布局回归；
6. `git diff --check` 无空白错误；
7. 未覆盖用户无关改动，未引入不必要依赖；
8. 交付说明列出改变、验证和已知边界，不夸大 P0 范围。
